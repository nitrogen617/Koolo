<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NIP Editor</title>
    <style>
        :root {
            --bg: #f4f6f9;
            --bg-accent: #e9edf3;
            --card: #ffffff;
            --text: #1f2a37;
            --muted: #667085;
            --line: #e3e8ef;
            --primary: #0f6fff;
            --primary-ink: #0b5fe0;
            --warning: #ffb020;
            --danger: #e5484d;
            --success: #16a34a;
            --shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
            --radius-lg: 18px;
            --radius-md: 12px;
            --radius-sm: 8px;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: "Pretendard", "SUIT", "Noto Sans KR", "Apple SD Gothic Neo", sans-serif;
            color: var(--text);
            background: radial-gradient(circle at top left, #f8fafc 0%, #f1f5f9 45%, #e2e8f0 100%);
        }

        .app {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .topbar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
            padding: 16px 24px;
            background: var(--card);
            box-shadow: 0 2px 10px rgba(15, 23, 42, 0.06);
            position: sticky;
            top: 0;
            z-index: 50;
        }

        .topbar-left {
            display: flex;
            align-items: center;
            gap: 16px;
            flex: 1;
        }

        .icon-btn {
            width: 44px;
            height: 44px;
            border-radius: 14px;
            border: 1px solid var(--line);
            background: var(--card);
            display: grid;
            place-items: center;
            cursor: pointer;
            transition: transform 0.15s ease, box-shadow 0.15s ease;
        }

        .icon-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 6px 14px rgba(15, 23, 42, 0.12);
        }

        .icon-btn.small {
            width: 34px;
            height: 34px;
            border-radius: 10px;
            font-size: 13px;
        }

        .search-shell {
            flex: 1;
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 16px;
            border-radius: 18px;
            border: 1px solid var(--line);
            background: #fff;
            box-shadow: inset 0 1px 2px rgba(15, 23, 42, 0.05);
        }

        .search-shell input {
            border: none;
            outline: none;
            width: 100%;
            font-size: 15px;
            background: transparent;
        }

        .topbar-right {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .btn {
            border: none;
            padding: 10px 16px;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.15s ease, box-shadow 0.15s ease;
        }

        .btn-primary {
            background: var(--primary);
            color: #fff;
            box-shadow: 0 8px 16px rgba(15, 111, 255, 0.2);
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 12px 20px rgba(15, 111, 255, 0.25);
        }

        .btn-ghost {
            background: var(--bg-accent);
            color: var(--text);
        }

        .select {
            padding: 10px 12px;
            border-radius: 12px;
            border: 1px solid var(--line);
            background: #fff;
        }

        .filterbar {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            padding: 14px 24px;
            background: var(--card);
            border-bottom: 1px solid var(--line);
        }

        .quality-tabs {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .content-header .filterbar {
            padding: 0;
            background: transparent;
            border-bottom: none;
        }

        .filter-chip {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 14px;
            border-radius: 999px;
            border: 1px solid var(--line);
            background: #fff;
            font-weight: 600;
            cursor: pointer;
        }

        .filter-chip.compact {
            padding: 6px 10px;
            font-size: 13px;
        }
        .filterbar .quality-tab {
            padding: 8px 14px;
            border-radius: 999px;
            border: 1px solid var(--line);
            background: #fff;
            font-weight: 600;
            cursor: pointer;
        }

        .filterbar .quality-tab.active {
            background: var(--primary);
            color: #fff;
            border-color: var(--primary);
        }

        .filter-chip.active {
            border-color: var(--primary);
            color: var(--primary);
            box-shadow: inset 0 0 0 1px rgba(15, 111, 255, 0.2);
        }

        .layout {
            display: grid;
            grid-template-columns: 280px 1fr;
            gap: 24px;
            padding: 24px;
        }

        .sidebar {
            background: var(--card);
            border-radius: var(--radius-lg);
            padding: 20px;
            box-shadow: var(--shadow);
            height: fit-content;
        }

        .sidebar-section {
            margin-bottom: 24px;
        }

        .sidebar-title {
            font-weight: 700;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .sidebar-title-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 8px;
        }

        .type-list {
            display: grid;
            gap: 10px;
        }

        .type-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 12px;
            border-radius: 12px;
            border: 1px solid transparent;
            cursor: pointer;
            transition: all 0.15s ease;
        }

        .type-item:hover {
            background: var(--bg-accent);
        }

        .type-item.active {
            border-color: var(--primary);
            background: rgba(15, 111, 255, 0.08);
            color: var(--primary-ink);
        }

        .type-item.recent {
            border-style: dashed;
            border-color: rgba(16, 185, 129, 0.45);
            background: rgba(16, 185, 129, 0.08);
            color: #0f5132;
            font-weight: 700;
        }

        .type-item.recent .muted {
            color: rgba(15, 81, 50, 0.7);
        }

        .content {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .content-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 16px;
        }

        .content-title {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 22px;
            font-weight: 800;
        }

        .content-selection {
            font-size: 14px;
            font-weight: 600;
            color: var(--muted);
        }

        .content-summary {
            color: var(--muted);
            font-size: 13px;
            font-weight: 600;
        }
        .quality-tabs {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .quality-tab {
            padding: 8px 14px;
            border-radius: 999px;
            border: 1px solid var(--line);
            background: #fff;
            font-weight: 600;
            cursor: pointer;
        }

        .quality-tab.active {
            background: var(--primary);
            color: #fff;
            border-color: var(--primary);
        }

        #typeSortToggle,
        #ruleSortToggle {
            min-width: 0;
            padding: 6px 12px;
            width: auto;
            height: auto;
            border-radius: 999px;
            font-size: 13px;
            font-weight: 700;
            background: #f8fafc;
            border: 1px solid rgba(15, 23, 42, 0.12);
            color: #1f2937;
            box-shadow: 0 1px 2px rgba(15, 23, 42, 0.08);
            letter-spacing: 0.2px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            line-height: 1;
        }

        #typeSortToggle:hover,
        #ruleSortToggle:hover {
            background: #eef2f7;
            border-color: rgba(15, 23, 42, 0.2);
        }

        #typeSortToggle:active,
        #ruleSortToggle:active {
            transform: translateY(1px);
        }

        .quality-tab.favorites {
            color: #b45309;
            border-color: rgba(180, 83, 9, 0.5);
            font-weight: 700;
        }

        .quality-tab.favorites.active {
            color: #92400e;
            border-color: rgba(180, 83, 9, 0.8);
            background: rgba(251, 191, 36, 0.2);
        }

        .rule-list {
            display: grid;
            gap: 16px;
        }

        .rule-card {
            background: var(--card);
            border-radius: var(--radius-md);
            padding: 18px 20px;
            box-shadow: var(--shadow);
            display: grid;
            gap: 12px;
            content-visibility: auto;
            contain-intrinsic-size: 160px;
        }

        .rule-card.disabled {
            opacity: 0.6;
        }

        .rule-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 12px;
        }

        .rule-title {
            font-weight: 700;
            font-size: 17px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .rule-comment {
            font-size: 11px;
            font-weight: 600;
            color: var(--muted);
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 1px 6px;
            border-radius: 6px;
            border: 1px dashed rgba(120, 132, 150, 0.35);
            background: transparent;
            cursor: pointer;
            font: inherit;
            appearance: none;
            line-height: 1.2;
        }

        .rule-comment.empty {
            color: rgba(120, 132, 150, 0.75);
        }

        .rule-comment-icon {
            width: 14px;
            height: 14px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 9px;
            font-weight: 700;
            color: var(--muted);
            border: 1px solid rgba(120, 132, 150, 0.45);
            border-radius: 50%;
            background: rgba(120, 132, 150, 0.08);
            line-height: 1;
        }

        .rule-meta {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .pill {
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 12px;
            font-weight: 600;
            background: var(--bg-accent);
            color: var(--muted);
        }

        .pill-toggle {
            border: 1px solid rgba(15, 23, 42, 0.12);
            background: #fff;
            cursor: pointer;
        }

        .pill-toggle.active {
            border-color: rgba(15, 111, 255, 0.4);
            background: rgba(15, 111, 255, 0.12);
            color: var(--primary-ink);
            font-weight: 700;
        }

        .pill.primary {
            background: rgba(15, 111, 255, 0.12);
            color: var(--primary-ink);
        }

        .pill.quality-normal,
        .pill.quality-base {
            background: #ffffff;
            color: #111827;
            border: 1px solid #e2e8f0;
        }

        .pill.quality-superior {
            background: #c0c0c0;
            color: #1f2937;
        }

        .pill.quality-magic {
            background: #6969ff;
            color: #ffffff;
        }

        .pill.quality-set {
            background: #00ff00;
            color: #0f172a;
        }

        .pill.quality-rare {
            background: #ffff77;
            color: #0f172a;
        }

        .pill.quality-unique {
            background: #bfa969;
            color: #1f2937;
        }

        .pill.quality-crafted {
            background: #ff8000;
            color: #1f2937;
        }

        .pill.quality-lowquality {
            background: #666666;
            color: #ffffff;
        }

        .rule-actions {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .toggle {
            width: 46px;
            height: 26px;
            border-radius: 999px;
            background: #cbd5e1;
            position: relative;
            cursor: pointer;
            transition: background 0.2s ease;
        }

        .toggle::after {
            content: "";
            position: absolute;
            top: 3px;
            left: 3px;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: #fff;
            transition: transform 0.2s ease;
            box-shadow: 0 2px 6px rgba(15, 23, 42, 0.2);
        }

        .toggle.active {
            background: var(--success);
        }

        .toggle.active::after {
            transform: translateX(20px);
        }

        .rule-body {
            display: flex;
            flex-direction: column;
            gap: 8px;
            color: var(--muted);
            font-size: 14px;
        }

        .rule-line {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            align-items: center;
            color: var(--text);
            font-weight: 500;
        }

        .value-chip {
            border: none;
            border-radius: 8px;
            padding: 2px 8px;
            background: rgba(15, 111, 255, 0.12);
            color: var(--primary-ink);
            font-weight: 700;
            cursor: pointer;
        }

        .value-input {
            width: 72px;
            padding: 4px 6px;
            border-radius: 8px;
            border: 1px solid var(--line);
            font-weight: 700;
        }

        .muted {
            color: var(--muted);
        }

        .empty-state {
            padding: 40px;
            text-align: center;
            border-radius: var(--radius-lg);
            background: var(--card);
            box-shadow: var(--shadow);
        }

        .toast {
            position: fixed;
            right: 24px;
            bottom: 24px;
            background: #111827;
            color: #fff;
            padding: 12px 16px;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(15, 23, 42, 0.2);
            opacity: 0;
            transform: translateY(10px);
            transition: all 0.2s ease;
            z-index: 1200;
        }

        .toast.show {
            opacity: 1;
            transform: translateY(0);
        }

        .modal {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(15, 23, 42, 0.5);
            align-items: center;
            justify-content: center;
            z-index: 1000;
        }

        .modal.active {
            display: flex;
        }

        .modal-content {
            background: #fff;
            padding: 24px;
            border-radius: 18px;
            width: min(600px, 92vw);
            box-shadow: var(--shadow);
        }

        .modal-content h2 {
            margin-top: 0;
        }

        .form-group {
            display: grid;
            gap: 8px;
            margin-bottom: 12px;
        }

        .form-input {
            padding: 10px 12px;
            border-radius: 12px;
            border: 1px solid var(--line);
            width: 100%;
        }

        .file-list {
            max-height: 240px;
            overflow-y: auto;
            border: 1px solid var(--line);
            border-radius: 12px;
            padding: 8px;
        }

        .file-item {
            padding: 8px 10px;
            border-radius: 10px;
            cursor: pointer;
        }

        .file-item:hover {
            background: var(--bg-accent);
        }

        @media (max-width: 1024px) {
            .layout {
                grid-template-columns: 1fr;
            }

            .sidebar {
                order: 2;
            }
        }

        @media (max-width: 720px) {
            .topbar {
                flex-direction: column;
                align-items: stretch;
            }

            .topbar-right {
                flex-wrap: wrap;
                justify-content: space-between;
            }

            .layout {
                padding: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="app">
        <header class="topbar">
            <div class="topbar-left">
                <button class="icon-btn" aria-label="back" onclick="history.back()">&#8592;</button>
                <div class="search-shell">
                    <input type="text" id="globalSearch" placeholder="Search items / rules..." />
                    <button class="icon-btn small" id="clearSearch" aria-label="clear">&#10005;</button>
                </div>
            </div>
            <div class="topbar-right">
                <button class="btn btn-ghost" onclick="showFileConfigModal()">Select Folder</button>
                <select class="select" id="pickitFile"></select>
                <button class="btn btn-ghost" onclick="loadPickitFile()">Load</button>
                <button class="btn btn-primary" onclick="createNewFile()">New File</button>
            </div>
        </header>

        <div class="filterbar">
            <div class="quality-tabs" id="qualityTabs"></div>
        </div>

        <div class="layout">
            <aside class="sidebar">
                <div class="sidebar-section">
                    <div class="sidebar-title-row">
                        <div class="sidebar-title">Types</div>
                        <button class="icon-btn small" id="typeSortToggle" title="Sort types">Sort</button>
                    </div>
                    <div class="type-list" id="typeList"></div>
                </div>
            </aside>

            <main class="content">
                <div class="content-header">
                    <div class="content-title"><span class="content-selection" id="selectionLabel">All</span></div>
                    <div class="content-summary" id="summaryCounts">Load a file to see counts.</div>
                    <div class="filterbar">
                        <div class="filter-chip compact active" id="filterStatus">All</div>
                        <div class="filter-chip compact" id="filterEthereal">Ethereal</div>
                        <div class="filter-chip compact" id="filterUnidentify">Unidentify</div>
                        <div class="filter-chip compact" id="filterFavorites">Favorites</div>
                        <div class="filter-chip compact active" id="filterComments">Comments</div>
                        <button class="icon-btn small" id="ruleSortToggle" title="Sort rules">A→Z</button>
                    </div>
                </div>

                <div class="rule-list" id="rulesList"></div>
            </main>
        </div>
    </div>

    <div id="fileConfigModal" class="modal">
        <div class="modal-content">
            <h2>Select Pickit Folder</h2>
            <p class="muted">Choose the folder containing .nip files.</p>
            <div class="form-group">
                <label for="pickitPath">Pickit Path</label>
                <div style="display: flex; gap: 8px;">
                    <input type="text" id="pickitPath" class="form-input" placeholder="e.g. G:\koolo\config\MyChar\pickit" />
                    <button class="btn btn-ghost" onclick="browseFolder()">Browse</button>
                </div>
            </div>
            <div class="form-group">
                <div id="foundFiles" class="file-list"></div>
            </div>
            <div style="display: flex; gap: 8px; justify-content: flex-end;">
                <button class="btn btn-ghost" onclick="scanForFiles()">Scan</button>
                <button class="btn btn-ghost" onclick="closeFileConfigModal()">Close</button>
                <button class="btn btn-primary" onclick="saveFileConfig()">Load</button>
            </div>
        </div>
    </div>

    <div id="ruleEditModal" class="modal">
        <div class="modal-content">
            <h2>Edit Rule</h2>
            <div class="form-group">
                <label for="ruleEditInput">NIP Line</label>
                <textarea id="ruleEditInput" class="form-input" rows="4"></textarea>
            </div>
            <div style="display: flex; gap: 8px; justify-content: flex-end;">
                <button class="btn btn-ghost" onclick="closeRuleEditModal()">Cancel</button>
                <button class="btn btn-primary" onclick="saveRuleEdit()">Save</button>
            </div>
        </div>
    </div>

    <div id="toast" class="toast"></div>

    <script src="/assets/js/Sortable.min.js"></script>
    <script src="/assets/js/pickit_editor.js"></script>
</body>
</html>
